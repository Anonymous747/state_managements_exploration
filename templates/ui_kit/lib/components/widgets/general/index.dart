import 'package:flutter/material.dart';

class PokemonLogoImage extends StatelessWidget {
  const PokemonLogoImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
