/// Use to avoid importing the same typedef
/// from UI dependencies
///
typedef VoidCallback = void Function();

/// Can be useful when you often use function with
/// String parameter
///
typedef StringBuilder = void Function(String);
